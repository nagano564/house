<!DOCTYPE html>
<html>
<head>
  <title>House</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <link href="https://fonts.googleapis.com/css?family=Lato" rel="stylesheet">
  <link href="//maxcdn.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css" rel="stylesheet">
  <link rel="stylesheet" type="text/css" href="http://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
</head>
<body>
  <div class="container">
    <ul class="nav nav-tabs">
      <div class="pull-right user-info">
        <% if user_signed_in? %>
          <li> Hello: <%= current_user.email %> - <%= link_to "Sign Out", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %></li>
      </div>
      <li class="<%= 'active' if current_page?(welcome_index_path) %>"><%= link_to "Home", welcome_index_path %></li>
      <li class="<%= 'active' if current_page?(welcome_lists_path) %>"><%= link_to "Info", welcome_lists_path %></li>
      <li class="<%= 'active' if current_page?(new_user_checklist_path) %>"><%= link_to "My List", new_user_checklist_path %></li>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle = "dropdown">Calc<span class="carat"></span></a>
        <ul class="dropdown-menu">
          <li><%= link_to "New Calculations", new_user_house_path(current_user.id) %></li>
          <li><%= link_to "Saved Calc", user_houses_path(current_user.id) %></li>
        </ul>
      </li>
      <li class="<%= 'active' if current_page?(todo_lists_path) %>"><%= link_to "Checklist", todo_lists_path %></li>
      <li class="<%= 'active' if current_page?(edit_user_registration_path) %>"><%= link_to "My Account", edit_user_registration_path %></li>
    </ul>
  <% else %>
      <div class="container">
      </div>
  </div>
  <% end %>
  <% if flash[:notice] %>
  <div class="alert alert-success">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <%= flash[:notice] %>
  </div>
<% elsif flash[:alert] %>
  <div class="alert alert-warning">
    <button type="button" class="close" data-dismiss="alert">&times;</button>
    <%= flash[:alert] %>
  </div>
<% end %>

  <%= yield %>

</body>
</html>
